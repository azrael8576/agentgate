name: Agent coupling audit

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run agent coupling audit
        run: bash scripts/audit_agent_coupling.sh
